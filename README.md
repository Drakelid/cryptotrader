# Cryptotrader

This project provides a minimal trading engine that consumes price data from a Binance WebSocket stream. Strategies implement the asynchronous `on_price_update` hook.

Run tests with `pytest`.
